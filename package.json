{
  "name": "brigade-noir-monorepo",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w frontend"
  }
}
